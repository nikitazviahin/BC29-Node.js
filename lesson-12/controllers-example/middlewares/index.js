const authorize = require("./authorize");
const upload = require("./upload");
const validationBody = require("./vaidationBody");
const isValidId = require("./isValidId");

module.exports = {
    authorize,
    upload,
    validationBody,
    isValidId,
}